import { initListingView } from '@components/ListingView/ListingView';

/**
 * NOVA Theme — Products Listing Page Script
 * Loaded only on src/views/pages/product/index.twig via {% block scripts %}.
 */

initListingView();
